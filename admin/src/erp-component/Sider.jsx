import styled from 'styled-components';
import { Layout } from 'antd';
const { Sider } = Layout;

const Index = styled(Sider).attrs()`
    background-color: #fff !important;
    border-right: 1px solid rgba(0, 0, 0, 0.10);
`

export default Index;
